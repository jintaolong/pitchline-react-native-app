export interface MatchEvent {
    time: string;
    event: string;
    icon: string;
    color: string;
}

export interface LineupPlayer {
    name: string;
    number: number;
}

export interface WordCloudWord {
    text: string;
    size: number;
    color: string;
}
